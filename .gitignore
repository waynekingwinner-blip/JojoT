node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
android

# The iOS project IS tracked — Info.plist carries the camera/photo usage
# strings and the portrait lock, and `cap add ios` would silently recreate
# them empty on another machine. Only its build output is ignored.
ios/App/Pods/
ios/App/App/public/
ios/App/build/
ios/App/output/
ios/App/App.xcodeproj/xcuserdata/
ios/App/App.xcworkspace/xcuserdata/
ios/DerivedData/

# TypeScript / Vite build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# IDE
.idea
*.iml

# e2e failure screenshots
e2e-failures/
scratch/

# RevenueCat key lives here — never committed
.env.local
.env*.local
